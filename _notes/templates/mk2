# mk2 => mk2 
./gradlew assembleDebug --configure-on-demand --parallel --daemon
adb install -r ./build/outputs/apk/blazer-loan-debug.apk

# ZX: On Linux and MacOS, use mk for test compile, and mk2 to compile and deploy
